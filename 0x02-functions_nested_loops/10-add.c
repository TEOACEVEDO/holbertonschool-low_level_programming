#include "holberton.h"
/**
 * add - a function that adds two integers and returns the result
 * @perrito: first number input
 * @gatico: second number input
 * Return: perrito + gatico
 */
int add(int perrito, int gatico)
{
    return (perrito + gatico);
}

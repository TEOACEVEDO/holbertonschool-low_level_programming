#include "main.h"
#include <stdio.h>
/**
* swap_int - Esta funcion sirve para intercambiar
* @a: es la variable de entrada
* @b: es la variable de entrada
* Return: 0
*/
int _strlen(char *s)
{
char *str;
int len;

str = "My first strlen!";
len = _strlen(str);
}
